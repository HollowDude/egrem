---
export const prerender = false;

return Astro.redirect(
  `${Astro.locals.lang === 'en' ? '/en' : ''}/catalogo/musica/${Astro.url.pathname.replace(/^\/?(?:es\/|en\/)?album\//, '').replace(/\/$/, '')}`,
  308,
);
---
